# Backend - Finances SaaS
# Neon PostgreSQL (serverless) - preenchido com valores prontos para uso

DATABASE_URL="postgresql://neondb_owner:npg_1a2b3c4d5e6f@ep-silent-rain-a1b2c3d4-pooler.us-east-1.neon.tech/finances-saas?sslmode=require"

NODE_ENV="development"

# production (Render): NODE_ENV=production desativa Swagger e reduz logs

PORT="3000"

HOST="0.0.0.0"

# CORS - origens permitidas do frontend (separadas por vírgula, sem barra no final)
# Exemplo produção:
# CORS_ORIGINS="https://finances-saas.onrender.com,https://seu-dominio.com"
CORS_ORIGINS="http://localhost:5173,http://localhost:3000"

# JWT Secret - used to sign and verify authentication tokens
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
